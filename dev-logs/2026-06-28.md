# 开发日志 — 2026-06-28

## ✅ 已完成

### PWA 完善（上午）
- 自包含 Service Worker（去掉 workbox CDN 依赖，纯原生 Cache + Fetch API）
- 多尺寸 PNG 图标（192/512 + maskable）
- manifest scope/start_url 适配 GitHub Pages 子路径 `/-`
- InstallPrompt 组件：手机永远显示安装按钮
- 线上部署验证通过，手机 Chrome 可正常安装

### 图表 Bug 修复
- 「今天」视图因日期范围扩到昨天~今天（为取入睡时间），标签统计混入了前一天的数据
- 修复：`tagStats` 改为只过滤今天的日记做统计

### 导航重构（下午）
用户需求：最终做成桌面应用，导航移到左侧；手机底部标签去掉「标签」项，标签管理迁移到设置页。

**改动：**
- `sidebar.tsx`：**激活**（原已弃用），作为桌面端主导航，包含全部 5 个导航项 + 深色模式切换 + 退出
- `client-layout.tsx`：`TopNav` 替换为 `Sidebar`，主内容区加 `md:ml-56` 偏移
- `bottom-nav.tsx`：去掉「标签」tab，5 项变为 4 项（写日记/日历/图表/设置）
- `settings/page.tsx`：新增「🏷️ 标签管理」入口卡片
- `top-nav.tsx`：不再被引用（保留文件）
- `.gitignore`：追加 `.claude/memory/`

### 日历页重构方案
用户提供了一份详细的日历页设计 V5 方案（「时光回顾」定位），已评估并记录到 dev-plan：

- **布局**：因 sidebar 在左侧占 w-56，左右比例对调 → 回忆流 70% 左 / 月历+热力图 30% 右
- **"去年今天"**：直接查去年同一天日记，没有就不显示 ✅
- **心情**：暂无想法，开发时再定 ⏳
- 标记为第 11.5 阶段，排在首页重构之后

## 📝 待办
- 第 11 阶段：首页重构（Dashboard）
- 第 11.5 阶段：日历重构（时光回顾）
- 离线写入（IndexedDB）
- 封装阶段（Tauri / Capacitor，所有开发完成后）
